name: Apply Android root swipe polish

on:
  push:
    branches:
      - ux/android-root-swipe-navigation-20260913
    paths:
      - .github/android-root-swipe.trigger

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Check out branch
        uses: actions/checkout@v6
        with:
          ref: ux/android-root-swipe-navigation-20260913

      - name: Apply guarded root swipe
        run: python3 .github/scripts/android_root_swipe_patch.py

      - name: Enable predictive Back contract
        run: python3 .github/scripts/android_predictive_back_patch.py

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1
          cache: true

      - name: Get packages
        run: flutter pub get

      - name: Format focused files
        run: dart format lib/features/design_v2/v2_workspace_preview.dart test/features/design_v2_workspace_test.dart test/features/android_predictive_back_contract_test.dart

      - name: Verify diff hygiene
        run: git diff --check

      - name: Run focused navigation tests
        run: flutter test test/features/design_v2_workspace_test.dart test/features/v2_final_ergonomics_test.dart test/features/v2_typed_organization_navigation_test.dart test/features/android_predictive_back_contract_test.dart

      - name: Analyze focused implementation
        run: flutter analyze lib/features/design_v2/v2_workspace_preview.dart test/features/design_v2_workspace_test.dart test/features/android_predictive_back_contract_test.dart

      - name: Remove temporary patch machinery
        run: rm -f .github/workflows/apply-android-root-swipe.yml .github/android-root-swipe.trigger .github/scripts/android_root_swipe_patch.py .github/scripts/android_predictive_back_patch.py

      - name: Verify final scope
        run: |
          git diff --name-only main...HEAD || true
          test ! -e .github/workflows/apply-android-root-swipe.yml
          test ! -e .github/scripts/android_root_swipe_patch.py
          test ! -e .github/scripts/android_predictive_back_patch.py

      - name: Commit polished source
        run: |
          git config user.name "xueqing-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "polish(android): add guarded root page swipe"
          git push origin HEAD:ux/android-root-swipe-navigation-20260913
