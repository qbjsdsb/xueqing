name: One-off V2 refresh and window polish

on:
  push:
    branches:
      - ui/v2-refresh-window-polish

permissions:
  contents: write

jobs:
  validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out branch
        uses: actions/checkout@v6
        with:
          ref: ui/v2-refresh-window-polish
          fetch-depth: 0

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1
          cache: true

      - name: Get packages
        run: flutter pub get

      - name: Apply refresh and window polish patch
        run: |
          python3 .github/scripts/prepare_v2_refresh_window_polish_patch.py
          python3 .github/scripts/patch_v2_refresh_window_polish.py

      - name: Format changed Dart files
        run: |
          dart format \
            lib/features/design_v2/v2_workspace_loader.dart \
            lib/features/design_v2/v2_management_page.dart \
            lib/features/design_v2/v2_workspace_preview.dart \
            lib/features/design_v2/v2_theme.dart \
            test/features/v2_refresh_window_polish_test.dart

      - name: Analyze
        run: flutter analyze

      - name: Run focused tests
        run: |
          flutter test test/features/v2_refresh_window_polish_test.dart
          flutter test test/features/design_v2_workspace_test.dart
          flutter test test/features/design_v2_workspace_loader_test.dart
          flutter test test/features/v2_shell_production_capabilities_test.dart

      - name: Run full Flutter test suite
        run: flutter test

      - name: Check patch whitespace
        run: git diff --check

      - name: Commit validated product changes and remove one-off machinery
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            lib/features/design_v2/v2_workspace_loader.dart \
            lib/features/design_v2/v2_management_page.dart \
            lib/features/design_v2/v2_workspace_preview.dart \
            lib/features/design_v2/v2_theme.dart \
            windows/runner/win32_window.cpp \
            test/features/v2_refresh_window_polish_test.dart
          git rm -f .github/scripts/prepare_v2_refresh_window_polish_patch.py
          git rm -f .github/scripts/patch_v2_refresh_window_polish.py
          git rm -f .github/workflows/oneoff-v2-refresh-window-polish.yml
          git diff --cached --check
          git commit -m 'UI V2: add refresh and quiet Windows polish'
          git push origin HEAD:ui/v2-refresh-window-polish
