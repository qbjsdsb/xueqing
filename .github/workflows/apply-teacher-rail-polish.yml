name: Apply teacher rail polish

on:
  pull_request:

permissions:
  contents: write

jobs:
  apply-and-verify:
    name: Apply and verify teacher rail polish
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Check out PR branch
        uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Apply focused teacher workspace patch
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          path = Path('lib/features/teacher_workspace/presentation/teacher_workspace_page.dart')
          text = path.read_text(encoding='utf-8')

          old_footer = """          Padding(
            padding: EdgeInsets.fromLTRB(
              extended ? AppSpacing.lg : AppSpacing.sm,
              AppSpacing.sm,
              AppSpacing.sm,
              AppSpacing.lg,
            ),
            child: Row(
              children: [
                Expanded(
                  child: Text(
                    extended ? '开发数据 · 仅当前权限范围' : '开发数据',
                    textAlign: extended ? TextAlign.start : TextAlign.center,
                    style: Theme.of(context).textTheme.bodySmall,
                  ),
                ),
                if (onCheckForUpdates != null)
                  IconButton(
                    tooltip: '检查更新',
                    onPressed: checkingForUpdates ? null : onCheckForUpdates,
                    icon: checkingForUpdates
                        ? const SizedBox(
                            width: 20,
                            height: 20,
                            child: CircularProgressIndicator(strokeWidth: 2),
                          )
                        : const Icon(Icons.system_update_alt),
                  ),
                if (extended && onSignOut != null)
                  IconButton(
                    tooltip: '退出登录',
                    onPressed: onSignOut,
                    icon: Icon(Icons.logout),
                  ),
              ],
            ),
          ),
"""
          new_footer = """          Padding(
            padding: EdgeInsets.fromLTRB(
              extended ? AppSpacing.lg : AppSpacing.sm,
              AppSpacing.sm,
              AppSpacing.sm,
              AppSpacing.lg,
            ),
            child: extended
                ? Row(
                    children: [
                      Expanded(
                        child: Text(
                          '权限内数据',
                          style: Theme.of(context).textTheme.bodySmall,
                        ),
                      ),
                      if (onCheckForUpdates != null)
                        IconButton(
                          tooltip: '检查更新',
                          onPressed: checkingForUpdates
                              ? null
                              : onCheckForUpdates,
                          icon: checkingForUpdates
                              ? const SizedBox(
                                  width: 20,
                                  height: 20,
                                  child: CircularProgressIndicator(
                                    strokeWidth: 2,
                                  ),
                                )
                              : const Icon(Icons.system_update_alt),
                        ),
                      if (onSignOut != null)
                        IconButton(
                          tooltip: '退出登录',
                          onPressed: onSignOut,
                          icon: const Icon(Icons.logout),
                        ),
                    ],
                  )
                : Column(
                    mainAxisSize: MainAxisSize.min,
                    children: [
                      if (onCheckForUpdates != null)
                        IconButton(
                          tooltip: '检查更新',
                          onPressed: checkingForUpdates
                              ? null
                              : onCheckForUpdates,
                          icon: checkingForUpdates
                              ? const SizedBox(
                                  width: 20,
                                  height: 20,
                                  child: CircularProgressIndicator(
                                    strokeWidth: 2,
                                  ),
                                )
                              : const Icon(Icons.system_update_alt),
                        ),
                      if (onSignOut != null)
                        IconButton(
                          tooltip: '退出登录',
                          onPressed: onSignOut,
                          icon: const Icon(Icons.logout),
                        ),
                    ],
                  ),
          ),
"""
          if text.count(old_footer) != 1:
              raise SystemExit(f'Expected one rail footer, found {text.count(old_footer)}')
          text = text.replace(old_footer, new_footer, 1)

          old_label = '              labelType: NavigationRailLabelType.none,\n'
          new_label = """              labelType: extended
                  ? NavigationRailLabelType.none
                  : NavigationRailLabelType.all,
"""
          if text.count(old_label) != 1:
              raise SystemExit(f'Expected one rail label mode, found {text.count(old_label)}')
          text = text.replace(old_label, new_label, 1)
          path.write_text(text, encoding='utf-8')
          PY

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1
          cache: true

      - name: Get packages
        run: flutter pub get

      - name: Format touched files
        run: dart format lib/features/teacher_workspace/presentation/teacher_workspace_page.dart lib/features/organization_management/presentation/organization_management_layout.dart

      - name: Verify formatting and analyze
        run: |
          git diff --check
          flutter analyze

      - name: Run full Flutter test suite
        run: flutter test

      - name: Remove one-shot workflow and commit verified changes
        shell: bash
        run: |
          rm .github/workflows/apply-teacher-rail-polish.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Polish teacher workspace rail and medium-width actions"
          git push origin HEAD:${{ github.head_ref }}
