name: Apply management navigation fix

on:
  push:
    branches:
      - fix/management-only-navigation

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/management-only-navigation

      - name: Apply exact two-file patch
        run: |
          python3 - <<'PY'
          from pathlib import Path

          workspace_path = Path('lib/features/teacher_workspace/presentation/teacher_workspace_page.dart')
          workspace = workspace_path.read_text(encoding='utf-8')
          class_start = workspace.index('class _WorkspaceShell extends StatelessWidget')
          return_pos = workspace.index('    return ResponsiveLayout(', class_start)
          guard = """    // A management-only account has one valid workspace surface. Flutter's
    // NavigationBar/NavigationRail require at least two destinations, so do
    // not render a navigation shell that would manufacture a second tab or
    // expose a teaching page outside the account's scope.
    if (!hasTeachingAccess && showManagement) {
      return Scaffold(
        appBar: AppBar(
          title: const Text('机构管理'),
          actions: [
            if (onCheckForUpdates != null)
              IconButton(
                tooltip: '检查更新',
                onPressed: checkingForUpdates ? null : onCheckForUpdates,
                icon: checkingForUpdates
                    ? const SizedBox(
                        width: 20,
                        height: 20,
                        child: CircularProgressIndicator(strokeWidth: 2),
                      )
                    : const Icon(Icons.system_update_alt),
              ),
            if (onSignOut != null)
              IconButton(
                tooltip: '退出登录',
                onPressed: onSignOut,
                icon: const Icon(Icons.logout),
              ),
          ],
        ),
        body: SafeArea(child: child),
      );
    }

"""
          if 'A management-only account has one valid workspace surface' in workspace:
              raise SystemExit('workspace guard already present')
          workspace_path.write_text(workspace[:return_pos] + guard + workspace[return_pos:], encoding='utf-8')

          test_path = Path('test/features/teacher_workspace_test.dart')
          tests = test_path.read_text(encoding='utf-8')
          marker = 'renders management-only accounts without a navigation assertion'
          if marker in tests:
              raise SystemExit('regression test already present')
          if not tests.endswith('}\n'):
              raise SystemExit('unexpected test file ending')
          test_block = r'''

  testWidgets(
    'renders management-only accounts without a navigation assertion',
    (tester) async {
      final workspace = _fixtureWorkspace();
      final repository = _FakeLearningRepository(
        TeacherWorkspace(
          viewerName: workspace.viewerName,
          organizationName: workspace.organizationName,
          organizationTimeZone: workspace.organizationTimeZone,
          hasTeachingAccess: false,
          organizationId: 'org-1',
          roles: const <String>['org_admin'],
          canManageOrganization: true,
          students: const <WorkspaceStudent>[],
          loadedAt: workspace.loadedAt,
        ),
      );

      await _pumpWorkspace(tester, repository);

      expect(find.text('机构管理尚未接通'), findsOneWidget);
      expect(find.byType(NavigationBar), findsNothing);
      expect(find.byType(NavigationRail), findsNothing);
    },
  );
'''
          test_path.write_text(tests[:-2] + test_block + '}\n', encoding='utf-8')
          Path('.github/workflows/zz-apply-management-navigation-fix.yml').unlink()
          PY

      - name: Verify and commit
        run: |
          git diff --check
          printf '%s\n' "$(git diff --name-only | sort)"
          test "$(git diff --name-only | sort | tr '\n' ' ')" = ".github/workflows/zz-apply-management-navigation-fix.yml lib/features/teacher_workspace/presentation/teacher_workspace_page.dart test/features/teacher_workspace_test.dart "
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: render management-only workspace without navigation'
          git push origin HEAD:fix/management-only-navigation
