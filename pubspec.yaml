name: xueqing
description: 学情闭环教师工作台
publish_to: "none"
version: 0.3.0+8

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.0.0
  convert: ^3.1.2
  crypto: ^3.0.7
  excel: ^4.0.6
  file_saver: ^0.4.0
  image_picker: ^1.2.3
  path_provider: ^2.1.6
  supabase_flutter: ^2.17.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
