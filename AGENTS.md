# AGENTS.md

本文件约束 Codex 与后续开发者在本仓库中的实现方式。

## 产品北极星

本项目是“机构教学协作与学生成长学情闭环管理平台”。优先解决真实教师工作流，不得将产品退化为 Excel 网页化或传统填表式教务系统。

## 不可破坏的业务约束

1. 一个学生在同一机构只有一个主档案。
2. 数据归机构，教师离职不得导致历史数据丢失。
3. 学情案例必须保留可追溯的生命周期和事件历史。
4. 原始事实只记录一次，周报/阶段报告等派生信息优先自动计算。
5. 未解决问题应支持明确的下一步行动。
6. 跨学科采用有限共享，不能默认让所有教师编辑全部学科数据。
7. 关键权限必须在数据库/RLS层执行，不能只靠前端隐藏按钮。
8. AI 不得静默修改正式学生学情状态，涉及正式结论必须由教师确认。

## 隐私与安全

- 禁止提交任何真实学生姓名、手机号、家长信息、试卷隐私内容到 GitHub。
- 禁止提交密码、API Key、Supabase service role key、数据库连接密码。
- 开发种子数据必须全部虚构。
- 涉及数据库 schema 的新增表，默认考虑 organization_id、created_at、updated_at、审计与 RLS。
- 删除核心业务数据前优先考虑软删除、停用或历史保留。

## 技术方向

- Flutter：Windows + Android 优先。
- Supabase：PostgreSQL + Auth + Storage + RLS。
- GitHub：版本管理、Issue、PR、CI。

在没有充分理由前，不要随意更换核心技术栈。

## 开发原则

- 优先完成小而完整的垂直闭环，不堆积大量半成品功能。
- UI 优先清晰、克制、低填写负担。
- 教师日常高频路径要减少点击与输入。
- 数据模型稳定性优先于短期页面便利。
- 对高风险迁移提供回滚或迁移方案。
- 每次较大功能应包含测试或明确验证步骤。

## V1 暂不做

- 排课收费 CRM
- 大型题库
- 学生成绩预测
- AI 自动诊断后直接改正式学情
- 家长独立 App
- 学生独立 App
- 大量第三方登录

如需改变上述范围，应先更新 `docs/PRODUCT.md` 与 `docs/ROADMAP.md`。
